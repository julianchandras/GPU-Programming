# GPU Reduction Benchmark

Files:
- `sum.h`
- `sum.c`
- `sum.cu`
- `benchmark.cu`
- `generate_dataset.py`
- `analyze_results.py`
- `Makefile`

Commands:
```bash
make all
make run
make plot
```

Adjust grid size (automatic in implementation, max 65536 blocks).
